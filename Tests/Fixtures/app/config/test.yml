framework:
    secret:        test
    test:          ~

# Doctrine Configuration
doctrine:
    dbal:
        driver:   "pdo_sqlite"
        path: "%kernel.root_dir%/sqlite.db"
        charset: UTF8

    orm:
        auto_generate_proxy_classes: "%kernel.debug%"
        auto_mapping: true
